# --- Core agent framework ---
langgraph>=0.2.60
langchain-core>=0.3.0

# --- Checkpointers (durable state) ---
langgraph-checkpoint-sqlite>=2.0.0      # local dev
langgraph-checkpoint-postgres>=2.0.0    # production (Supabase)
psycopg[binary,pool]>=3.2               # postgres driver used by the postgres saver

# --- API server ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9    # multipart/form-data file uploads
pydantic>=2.7

# --- Config ---
python-dotenv>=1.0

# --- Tests ---
pytest>=8.0

# --- Step 3: multimodal ingestion (optional — used only if GOOGLE_API_KEY is set) ---
google-genai>=0.3.0
